# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
*.egg

# Environment
.env
.env.*

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors
.vscode/
.idea/
*.swp
*.swo

# Logs (keep folder, ignore content)
logs/*.log

# Distribution
*.tar.gz
*.zip

# RAG vector index — committed to git after first build so users get it pre-built.
# Only ignored temporarily if you need to force a clean rebuild.
# rag/index/

# Claude Code local settings (user-specific paths)
.claude/

# User-specific config (hostnames, backend, model selection)
# Copy config.example.json → config.json and edit locally
config.json
ollama_key
logs/crash_recovery.json
rag/candidates.json
rag/failed.json

# Phase D — raw transcripts (large, ephemeral; only processed patterns go in git)
docs/transcripts/

# Phase D — generated patterns file starts as local-only until reviewed
# docs/flame_youtube_patterns.md  # uncomment to exclude until reviewed
